{
  "name": "nebula-expense-tracker-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client && mkdir -p public && cp -r client/dist/* public/"
  }
}
