{
  "name": "nebula-expense-tracker-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  }
}
